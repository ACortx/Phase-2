// java script initial example

//selecting button by id
const button = document.getElementById("myButton");

//add event listener to button, for when clicked
button.addEventListener("click", function () {
  alert("Here's what's inside the button itself");
});
